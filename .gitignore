# macOS / editor noise
.DS_Store
.openclaw/

# Local auth / secrets
auth/*.json
auth/*.txt
gmail_token.json
gmail-token*.json
.env
.env.*

# Runtime caches / logs
__pycache__/
*.pyc
*.pyo
*.log
.firebase/hosting..cache
.firebase/

# Dependencies
node_modules/
extractor/node_modules/
functions/node_modules/

# Local frontend build output
dist/
_site/

# Local Supabase CLI state
supabase/.temp/

# Generated local data
data/*.log
data/backup_*.json
data/cards_before_*.json
gmail_leads.json

# Local one-off business docs that should not flood GitHub
/*.pdf
Invoice*.pdf
invoices/
docs/*.pdf
!docs/SINGLE_TIER.pdf
docs/*.html

# Keep real source files tracked
!package.json
!package-lock.json
!functions/package.json
!functions/package-lock.json

# Local agent brain (ops/) is now tracked so it travels with the repo for handoff.
# Secrets/logs stay out via ops/.gitignore (.env, *.log, PAUSE, __pycache__).

# loose token file — never commit
lead-ingest-token.txt
